package edu.gatech.seclass.jobcompare6300;

import android.content.ContentValues;
import android.database.sqlite.SQLiteDatabase;

import edu.gatech.seclass.jobcompare6300.db.CurrentJobTable;
import edu.gatech.seclass.jobcompare6300.db.DbHelper;
import edu.gatech.seclass.jobcompare6300.db.SettingsTable;

public class TemplateCodeSH {
/*    DbHelper helper = new DbHelper();
    SQLiteDatabase db = helper.getWritableDatabase();
    ContentValues values = new ContentValues();
    values.put(SettingsTable.FeedEntry.COLUMN_NAME_salary_weight, settings.getTitle());
    values.put(SettingsTable.FeedEntry.COLUMN_NAME_bonus_weight, settings.getTitle());
    values.put(SettingsTable.FeedEntry.COLUMN_NAME_telework_weight, settings.getTitle());
    values.put(SettingsTable.FeedEntry.COLUMN_NAME_leave_time_weight, settings.getTitle());
    values.put(SettingsTable.FeedEntry.COLUMN_NAME_gym_weight, settings.getTitle());

    long newRowID = db.insert(SettingsTable.FeedEntry.TABLE_NAME, null, values);*/
}
